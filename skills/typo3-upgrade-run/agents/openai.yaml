interface:
  display_name: "TYPO3 14 Update"
  short_description: "Prove-invisible v12/v13 to 14.3 update in local DDEV, then optional elevation"
  default_prompt: "Use $typo3-upgrade-run to update this TYPO3 v12/v13 project to 14.3 LTS inside the local DDEV environment: seal the original baseline before any change, run the determinism self-test first, document every loop under .typo3-update, and close Contract A only at zero unexplained differences. After closure, run only explicitly approved Contract B tracks with separate intent and observed-result acceptance. Do not deploy."
