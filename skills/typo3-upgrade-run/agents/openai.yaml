interface:
  display_name: "TYPO3 14 Update"
  short_description: "Prove-invisible v12/v13 to 14.3 update in local DDEV, then optional elevation"
  default_prompt: "Use $typo3-upgrade-run to update this TYPO3 v12/v13 project to 14.3 LTS inside local DDEV within the hard 14-hour overnight budget. Seal Baseline A and prove determinism first. Let the upgrade run own the only bounded fix loop: routed skills do one pass and return findings. During iterations check affected pages plus seeded sentinels in the default interaction state; use exactly default, keyboard-focus, and nav-open for baseline, exhaustive determinism, and final closure. Reserve the last four hours for closure, snapshot only before stateful operations, route optional checks by actual impact, and close Contract A at zero unexplained differences. Run Contract B only if separately requested. Do not deploy."
